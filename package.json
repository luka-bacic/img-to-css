{
  "name": "img-to-css",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://luka-bacic.github.io/img-to-css/",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}
